/**
 * Point d'entrée du châssis applicatif.
 *
 * L'implémentation vit dans `shell/AppShell` ; ce fichier est conservé comme
 * point de montage des routes (`<Route element={<Layout />}>`).
 */
export { default } from './shell/AppShell';
